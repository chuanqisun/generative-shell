{
  "name": "serve",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun --watch server/index.ts -- temp",
    "check": "bun run format && bun run build && bun run test",
    "format": "prettier --write server/**/* web/**/*",
    "build": "tsc && bun build",
    "test": "bun test"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/node": "^26.2.0",
    "prettier": "^3.9.6"
  },
  "dependencies": {
    "@google/genai": "^2.18.0",
    "@xterm/addon-fit": "^0.11.0",
    "@xterm/xterm": "^6.0.0",
    "lit": "^3.3.3",
    "rxjs": "^7.8.2"
  }
}